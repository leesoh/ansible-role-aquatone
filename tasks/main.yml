---
- name: Install dependencies
  become: true
  when: ansible_os_family == "Debian"
  apt:
    name: "{{ item }}"
    update_cache: yes
    state: latest

- name: Install Aquatone
  become: true
  gem:
    name: aquatone
    state: latest
    user_install: no
